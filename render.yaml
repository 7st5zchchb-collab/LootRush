services:
  - type: web
    name: lootrush-2
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: FRONTEND_URL
        value: https://lootrush-2.onrender.com
